import XCTest
@testable import playground_swift

final class playground_swiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(playground_swift().text, "Hello, World!")
    }
}
