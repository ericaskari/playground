{
  "name": "lerna-playground",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "init": "lerna bootstrap",
    "build:proto:builder": "lerna run build:proto:builder",
    "build:proto:module": "lerna run build:proto:module",
    "start": "lerna run start --stream",
    "init:hoist": "lerna bootstrap --hoist",
    "pre-commit": "lerna run precommit --stream"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "typescript": "^4.1.2"
  }
}
